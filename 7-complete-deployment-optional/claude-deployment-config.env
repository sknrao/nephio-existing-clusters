# Nephio Deployment Configuration
# Copy this to deployment-config.env and customize for your environment

#==============================================================================
# GitHub Configuration
#==============================================================================
GITHUB_ORG="your-github-username"  # Your GitHub username or organization

#==============================================================================
# Cluster Contexts
# Run 'kubectl config get-contexts' to see available contexts
#==============================================================================
RAN_CONTEXT="my-ran"              # Context name for RAN cluster
CORE_CONTEXT="my-core"            # Context name for CORE cluster
MGMT_CONTEXT="nephio-mgmt"        # Management cluster (optional, for GitOps)

#==============================================================================
# Git Repositories
# These will be used/created based on deployment mode
#==============================================================================
BLUEPRINTS_REPO="nephio-blueprints"  # Upstream package repository
RAN_REPO="nephio-my-ran"             # RAN cluster deployment repo
CORE_REPO="nephio-my-core"           # CORE cluster deployment repo

#==============================================================================
# RAN Cluster Configuration
#==============================================================================
RAN_CLUSTER_NAME="my-ran"         # Logical cluster name
RAN_SITE_ID="site-ran-01"         # Site identifier
RAN_NODE_NAME=""                  # Node name (leave empty to auto-detect)

#==============================================================================
# CORE Cluster Configuration
#==============================================================================
CORE_CLUSTER_NAME="my-core"       # Logical cluster name
CORE_SITE_ID="site-core-01"       # Site identifier
CORE_NODE_NAME=""                 # Node name (leave empty to auto-detect)

#==============================================================================
# Network Configuration
# Adjust these to match your LAN setup
#==============================================================================

# Control Plane Network (N2, N4, SBI interfaces)
CONTROL_PLANE_CIDR="192.168.10.0/24"
CONTROL_PLANE_GATEWAY="192.168.10.1"

# User Plane Network (N3, N6 interfaces)
USER_PLANE_CIDR="192.168.20.0/24"
USER_PLANE_GATEWAY="192.168.20.1"

# Physical Interface
# Run 'ip addr' on your nodes to find the correct interface
PHYSICAL_INTERFACE="eth0"         # Common: eth0, ens33, enp1s0, eno1

# MTU
MTU="1500"                        # Standard Ethernet MTU

#==============================================================================
# Deployment Mode
#==============================================================================
# Options:
#   manual  - Direct kubectl apply (recommended for bare-metal)
#   gitops  - Use Nephio PackageVariants (requires management cluster)
DEPLOYMENT_MODE="manual"

#==============================================================================
# Git Authentication (only for GitOps mode with private repos)
#==============================================================================
GIT_USERNAME=""                   # GitHub username
GIT_TOKEN=""                      # Personal Access Token with 'repo' scope

#==============================================================================
# Feature Flags
#==============================================================================
SKIP_VERIFICATION="false"         # Set to "true" to skip post-deployment checks
VERBOSE="false"                   # Set to "true" for detailed output
DRY_RUN="false"                   # Set to "true" to preview without applying